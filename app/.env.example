# DeepSeek API key used by the generation backend (api/generate.js).
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# Optional: override the DeepSeek model (default: deepseek-v4-pro).
# DEEPSEEK_MODEL=deepseek-v4-pro
