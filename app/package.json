{
  "name": "pitchaprint",
  "version": "0.1.0",
  "private": true,
  "description": "Text-to-hardware generator — type a device description, get a 3D enclosure, circuit, firmware, BOM and assembly instructions.",
  "scripts": {
    "dev": "node dev-server.js",
    "start": "node dev-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  }
}
